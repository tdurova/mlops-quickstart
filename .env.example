# Application
APP_ENV=local
APP_PORT=8080
LOG_LEVEL=info

# Database (matches docker compose service `db`)
DATABASE_URL=postgresql+psycopg://mlops_user:mlops_password@db:5432/mlops_db

# Optional: experiment tracking / feature flags
# MLFLOW_TRACKING_URI=http://mlflow:5000
# FEATURE_FLAG_EXPERIMENTAL_MODEL=false
