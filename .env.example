# Application
APP_PORT=8080
APP_ENV=local
LOG_LEVEL=info
SERVICE_NAME=mlops-quickstart
APP_VERSION=dev

# Optional Postgres settings (compose.yaml includes a placeholder Postgres service).
# The current demo app does not connect to a database.
POSTGRES_USER=mlops_user
POSTGRES_PASSWORD=mlops_password
POSTGRES_DB=mlops_db
