APP_PORT=8080

# Optional Postgres settings (used only if you extend the demo to talk to the DB
# defined in compose.yaml).
POSTGRES_USER=mlops_user
POSTGRES_PASSWORD=mlops_password
POSTGRES_DB=mlops_db

